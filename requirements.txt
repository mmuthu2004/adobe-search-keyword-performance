# Lambda runtime dependencies
# boto3 is pre-installed in the AWS Lambda Python 3.12 runtime but pinned
# here to ensure local development and CI/CD match the deployed version.
boto3==1.42.56
pyyaml==6.0.3
