# boto3 is pre-installed in the AWS Lambda Python runtime
# No additional dependencies needed for the Lambda path
# pandas is only required for the EMR Serverless path (not Lambda)
boto3>=1.34.0
pyyaml>=6.0.1
